# Menu

Any content file could be added to the main menu by adding `menu` entry to the
front header of the file.

The optional attribute `weight` specifies the order in list of all menu items. The
lower value -> more to the left.

```yaml
---
menu:
  main:
    weight: 10
---
```
